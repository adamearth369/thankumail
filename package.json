{
  "name": "thankumail",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "npm run build:server",
    "build:server": "npx esbuild server/index.ts --bundle --platform=node --format=cjs --outfile=dist/index.cjs",
    "start": "node dist/index.cjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "drizzle-orm": "^0.33.0",
    "express": "^4.19.2",
    "nodemailer": "^6.9.16",
    "pg": "^8.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
