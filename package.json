{
  "name": "thankumail",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "npm run build:server",
    "build:server": "node -e \"const fs=require('fs');const hasServer=fs.existsSync('server/index.ts');const hasSrc=fs.existsSync('src/index.ts');if(!hasServer&&!hasSrc){console.error('Missing entry: server/index.ts or src/index.ts');process.exit(1)}\" && (npx esbuild server/index.ts --bundle --platform=node --format=cjs --outfile=dist/index.cjs || npx esbuild src/index.ts --bundle --platform=node --format=cjs --outfile=dist/index.cjs)",
    "start": "node dist/index.cjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
